"""Architecture lives in :mod:`libreyolo.models.librevocab1.sam3` (vendored from
``mranzinger/sam3-radio``) and is composed by
:func:`libreyolo.models.librevocab1.builder.build_librevocab1`.

This file exists only as a redirect. The earlier scaffold described a custom
two-stream / Grounding-DINO-style design (Path B/C in the proposal); that path
was abandoned in favor of Path A — direct SAM3 + CRADIOv4 vision swap, the
configuration NVIDIA validates in figures 6–8 of arXiv 2601.17237.
"""
